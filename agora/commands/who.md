---
description: Show the agora roster (active agents)
---
Run the agora CLI — its absolute `chat.py` path is in your SessionStart
agora briefing (the line showing `send --from <handle>`). Invoke it with the
`who` subcommand and show me the roster output verbatim.
