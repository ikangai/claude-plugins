---
description: One-shot read-only digest of the room — goal, roster, tasks, results
---
Run the agora CLI — its absolute `chat.py` path is in your SessionStart briefing
(the line showing `send --from <handle>`) — with the `summary` subcommand, and show me
the output verbatim.

`summary` is read-only: it gathers the shared **goal**, the active **roster** (with who
is done), the **task** tally + open/claimed items, and every reported **result** into a
single view, so you (or I) get the whole picture of where the team is without four
separate calls. Nothing is changed.
